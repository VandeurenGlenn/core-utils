#! /usr/bin/env node 

import { program } from "commander";
import { execute } from "./../utils.js";

const args = process.argv.slice(2, process.argv.length)
program
  .command('dir')
  .description("")
  .option('-cftuSUX nor --sort.', `-cftuSUX nor --sort.`)
	.option('-a, --all', `-a, --all`)
	.option('-A, --almost-all', `-A, --almost-all`)
	.option('--author', `with -l, print the author of each file`)
	.option('-b, --escape', `-b, --escape`)
	.option('--block-size=SIZE', `use SIZE-byte blocks`)
	.option('-B, --ignore-backups', `-B, --ignore-backups`)
	.option('-c     with -lt: sort by, and show, ctime (time of  last', `-c     with -lt: sort by, and show, ctime (time of  last`)
	.option('-C     list entries by columns', `-C     list entries by columns`)
	.option('--color[=WHEN]', `control whether color is used to distinguish file`)
	.option('-d, --directory', `-d, --directory`)
	.option('-D, --dired', `-D, --dired`)
	.option('-f     do not sort, enable -aU, disable -lst', `-f     do not sort, enable -aU, disable -lst`)
	.option('-F, --classify', `-F, --classify`)
	.option('--format=WORD', `across  -x,  commas  -m,  horizontal -x, long -l,`)
	.option('--full-time', `like -l --time-style=full-iso`)
	.option('-g     like -l, but do not list owner', `-g     like -l, but do not list owner`)
	.option('-G, --no-group', `-G, --no-group`)
	.option('-h, --human-readable', `-h, --human-readable`)
	.option('--si', ``)
	.option('-H, --dereference-command-line', `-H, --dereference-command-line`)
	.option('--dereference-command-line-symlink-to-dir', `follow  each  command  line  symbolic  link  that`)
	.option('--hide=PATTERN', `do not list implied entries matching  shell  PAT-`)
	.option('--indicator-style=WORD  append indicator with style WORD', `to entry names:`)
	.option('-i, --inode', `-i, --inode`)
	.option('-I, --ignore=PATTERN', `-I, --ignore=PATTERN`)
	.option('-k     like --block-size=1K', `-k     like --block-size=1K`)
	.option('-l     use a long listing format', `-l     use a long listing format`)
	.option('-L, --dereference', `-L, --dereference`)
	.option('-m     fill width with a comma separated list of entries', `-m     fill width with a comma separated list of entries`)
	.option('-n, --numeric-uid-gid', `-n, --numeric-uid-gid`)
	.option('-N, --literal', `-N, --literal`)
	.option('-o     like -l, but do not list group information', `-o     like -l, but do not list group information`)
	.option('-p, --file-type', `-p, --file-type`)
	.option('-q, --hide-control-chars', `-q, --hide-control-chars`)
	.option('--show-control-chars', `show non graphic characters as-is (default unless`)
	.option('-Q, --quote-name', `-Q, --quote-name`)
	.option('--quoting-style=WORD', `use  quoting style WORD for entry names: literal,`)
	.option('-r, --reverse', `-r, --reverse`)
	.option('-R, --recursive', `-R, --recursive`)
	.option('-s, --size', `-s, --size`)
	.option('-S     sort by file size', `-S     sort by file size`)
	.option('--sort=WORD', `extension -X, none -U, size -S, time -t,  version`)
	.option('-v,  status -c, time -t, atime -u, access -u, use', `-v,  status -c, time -t, atime -u, access -u, use`)
	.option('-u', `-u`)
	.option('--time=WORD', `with -l, show time as WORD instead  of  modifica-`)
	.option('--time-style=STYLE', `with -l, show times using style STYLE:  full-iso,`)
	.option('-t     sort by modification time', `-t     sort by modification time`)
	.option('-T, --tabsize=COLS', `-T, --tabsize=COLS`)
	.option('-u     with -lt: sort by, and show, access time with -l:', `-u     with -lt: sort by, and show, access time with -l:`)
	.option('-U     do not sort; list entries in directory order', `-U     do not sort; list entries in directory order`)
	.option('-v     sort by version', `-v     sort by version`)
	.option('-w, --width=COLS', `-w, --width=COLS`)
	.option('-x     list entries by lines instead of by columns', `-x     list entries by lines instead of by columns`)
	.option('-X     sort alphabetically by entry extension', `-X     sort alphabetically by entry extension`)
	.option('-1     list one file per line', `-1     list one file per line`)
	.option('--help', ``)
	.option('--version', `output version information and exit`)
  .action(() => execute('dir', args))
    
    